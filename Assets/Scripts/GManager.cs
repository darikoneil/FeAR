using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// This 'GManager' class is the manager class for the GUI on Display 2.
/// It handles realtime experimenter feedback and setting selections.
/// </summary>
public class GManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
